# 外贸员供应商资源库 + AI 智能匹配 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为所有用户新增供应商资源库管理功能，并在公告详情页为未绑定供应商的用户提供 AI 智能匹配（从资源库推荐 Top 3 供应商）。

**Architecture:** 新增 `crm_user_supplier_pool` 表关联用户与供应商/诊断数据；后端复用现有 AI 评分引擎（prompt + LLM 调用 + 缓存）扩展为多供应商批量评分；前端新增资源库管理页和 AiMatchCard 组件，通过 `supplier_id` 分流企业/个人用户。

**Tech Stack:** Next.js App Router, mysql2, ECharts, shadcn/ui, lucide-react, i18n (6 语言)

---

## 文件结构

### 新增文件

| 文件 | 职责 |
|------|------|
| `src/lib/db/migrations/083-user-supplier-pool.ts` | 数据库迁移：创建 crm_user_supplier_pool 表 |
| `src/lib/repos/user-supplier-pool.repo.ts` | 资源库数据访问层（CRUD + JOIN 查询） |
| `src/app/api/user/supplier-pool/route.ts` | 资源库列表 + 添加 API |
| `src/app/api/user/supplier-pool/[id]/route.ts` | 单条编辑（备注）+ 删除 API |
| `src/lib/services/ai-match/index.ts` | AI 智能匹配服务编排（多供应商批量评分） |
| `src/app/api/notices/[id]/ai-match/route.ts` | AI 匹配 API 端点 |
| `src/features/procurement/api/ai-match.ts` | 前端 API 客户端 |
| `src/features/procurement/hooks/useAiMatch.ts` | 前端数据 Hook |
| `src/features/procurement/components/AiMatchCard.tsx` | AI 匹配结果卡片组件 |
| `src/app/(public)/settings/supplier-pool/page.tsx` | 资源库管理页（Shell） |
| `src/app/(public)/settings/supplier-pool/page-client.tsx` | 资源库管理页（Client） |

### 修改文件

| 文件 | 变更 |
|------|------|
| `src/lib/db/schema.ts` | 注册 083 迁移 |
| `src/app/(public)/settings/layout.tsx` | 添加"供应商资源库"导航项 |
| `src/core/i18n/locales/zh/common.json` | 中文 i18n key |
| `src/core/i18n/locales/en/common.json` | 英文 i18n key |
| `src/core/i18n/locales/fr/common.json` | 法文 i18n key |
| `src/core/i18n/locales/ru/common.json` | 俄文 i18n key |
| `src/core/i18n/locales/es/common.json` | 西班牙文 i18n key |
| `src/core/i18n/locales/ar/common.json` | 阿拉伯文 i18n key |

---

## Task 1: 数据库迁移 — 创建 crm_user_supplier_pool 表

**Files:**
- Create: `src/lib/db/migrations/083-user-supplier-pool.ts`
- Modify: `src/lib/db/schema.ts`

- [ ] **Step 1: 创建迁移文件**

```typescript
// src/lib/db/migrations/083-user-supplier-pool.ts
/**
 * 083: 用户供应商资源库
 * user-supplier-pool
 *
 * 新增 crm_user_supplier_pool 表，供所有用户（企业+个人）管理合作工厂列表，
 * 用于 AI 智能匹配（从资源库推荐 Top N 供应商）。
 */
import type { Pool } from "mysql2/promise";
import { type Migration } from "./runner";

export const migration: Migration = {
  version: 83,
  name: "user-supplier-pool",
  async up(dbPool: Pool) {
    await dbPool.query(`
      CREATE TABLE IF NOT EXISTS crm_user_supplier_pool (
        id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
        user_id BIGINT UNSIGNED NOT NULL COMMENT '所属用户',
        supplier_id BIGINT UNSIGNED NULL COMMENT '关联 supplier 表',
        qualification_id BIGINT UNSIGNED NULL COMMENT '关联 crm_supplier_qualification 表',
        source VARCHAR(20) NOT NULL DEFAULT 'manual' COMMENT '来源: platform/manual',
        notes TEXT NULL COMMENT '私有备注',
        created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE KEY uk_user_supplier (user_id, supplier_id),
        UNIQUE KEY uk_user_qualification (user_id, qualification_id),
        KEY idx_user (user_id)
      ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    `);
    console.log("[migration-083] 用户供应商资源库表已就绪");
  },
};
```

- [ ] **Step 2: 在 schema.ts 注册迁移**

在 `src/lib/db/schema.ts` 中添加 import 和数组注册（在 m082 之后）：

```typescript
import { migration as m083 } from "./migrations/083-user-supplier-pool";
```

在 `ALL_MIGRATIONS` 数组末尾追加 `m083`。

- [ ] **Step 3: 提交**

```bash
git add src/lib/db/migrations/083-user-supplier-pool.ts src/lib/db/schema.ts
git commit -m "feat: 新增用户供应商资源库表迁移 (083)"
```

---

## Task 2: 资源库数据访问层 (Repository)

**Files:**
- Create: `src/lib/repos/user-supplier-pool.repo.ts`
- Test: `tests/unit/repos/user-supplier-pool.repo.test.ts`

- [ ] **Step 1: 编写测试**

```typescript
// tests/unit/repos/user-supplier-pool.repo.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { UserSupplierPoolRepo } from "@/lib/repos/user-supplier-pool.repo";

describe("UserSupplierPoolRepo", () => {
  const mockQuery = vi.fn();
  const mockExecute = vi.fn();
  const mockPool = { query: mockQuery, execute: mockExecute } as any;
  let repo: UserSupplierPoolRepo;

  beforeEach(() => {
    repo = new UserSupplierPoolRepo(mockPool);
    mockQuery.mockReset();
    mockExecute.mockReset();
  });

  it("listByUser 查询用户资源库列表", async () => {
    const fakeRows = [
      { pool_id: 1, supplier_id: 10, company: "测试工厂", industry: "电子", has_qualification: 1, notes: "备注" },
    ];
    mockQuery.mockResolvedValue([fakeRows]);
    const result = await repo.listByUser(100);
    expect(result).toHaveLength(1);
    expect(result[0].company).toBe("测试工厂");
    expect(mockQuery).toHaveBeenCalledTimes(1);
  });

  it("addFromPlatform 插入平台匹配记录", async () => {
    mockExecute.mockResolvedValue([{ insertId: 5 }]);
    await repo.addFromPlatform(100, 10, 20);
    expect(mockExecute).toHaveBeenCalledTimes(1);
    const sql = mockExecute.mock.calls[0][0];
    expect(sql).toContain("INSERT IGNORE INTO crm_user_supplier_pool");
  });

  it("addManual 插入手动添加记录", async () => {
    mockExecute.mockResolvedValue([{ insertId: 6 }]);
    await repo.addManual(100, 30);
    expect(mockExecute).toHaveBeenCalledTimes(1);
  });

  it("updateNotes 更新备注", async () => {
    mockExecute.mockResolvedValue([{ affectedRows: 1 }]);
    await repo.updateNotes(100, 5, "新备注");
    expect(mockExecute).toHaveBeenCalledTimes(1);
    const sql = mockExecute.mock.calls[0][0];
    expect(sql).toContain("UPDATE crm_user_supplier_pool SET notes");
  });

  it("remove 删除记录", async () => {
    mockExecute.mockResolvedValue([{ affectedRows: 1 }]);
    await repo.remove(100, 5);
    expect(mockExecute).toHaveBeenCalledTimes(1);
  });

  it("countByUser 统计数量", async () => {
    mockQuery.mockResolvedValue([[{ cnt: 3 }]]);
    const count = await repo.countByUser(100);
    expect(count).toBe(3);
  });

  it("linkQualification 回写诊断记录关联", async () => {
    mockExecute.mockResolvedValue([{ affectedRows: 1 }]);
    await repo.linkQualification(100, 5, 99);
    expect(mockExecute).toHaveBeenCalledTimes(1);
    const sql = mockExecute.mock.calls[0][0];
    expect(sql).toContain("qualification_id = ?");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/unit/repos/user-supplier-pool.repo.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 Repository**

```typescript
// src/lib/repos/user-supplier-pool.repo.ts
/**
 * 用户供应商资源库数据访问层
 * @module lib/repos/user-supplier-pool.repo
 */
import type { Pool, RowDataPacket } from "mysql2/promise";

export interface PoolSupplierItem {
  pool_id: number;
  supplier_id: number | null;
  qualification_id: number | null;
  company: string;
  industry: string;
  has_qualification: number;
  notes: string | null;
  source: string;
  created_at: string;
}

export class UserSupplierPoolRepo {
  constructor(private pool: Pool) {}

  /** 列出用户资源库（JOIN supplier 获取公司名和行业） */
  async listByUser(userId: number): Promise<PoolSupplierItem[]> {
    const [rows] = await this.pool.query(
      `SELECT p.id AS pool_id, p.supplier_id, p.qualification_id,
              COALESCE(s.company, '') AS company,
              COALESCE(s.industry, '') AS industry,
              CASE WHEN q.id IS NOT NULL THEN 1 ELSE 0 END AS has_qualification,
              p.notes, p.source, p.created_at
       FROM crm_user_supplier_pool p
       LEFT JOIN supplier s ON s.id = p.supplier_id
       LEFT JOIN crm_supplier_qualification q ON q.id = p.qualification_id
       WHERE p.user_id = ?
       ORDER BY p.created_at DESC`,
      [userId],
    );
    return (rows as RowDataPacket[]) as PoolSupplierItem[];
  }

  /** 从平台目录添加（supplier_id 有值） */
  async addFromPlatform(userId: number, supplierId: number, qualificationId: number | null): Promise<number> {
    const [result] = await this.pool.execute(
      `INSERT IGNORE INTO crm_user_supplier_pool (user_id, supplier_id, qualification_id, source)
       VALUES (?, ?, ?, 'platform')`,
      [userId, supplierId, qualificationId],
    );
    return Number((result as any).insertId ?? 0);
  }

  /** 手动添加（supplier_id 有值，指向新创建的 supplier 记录） */
  async addManual(userId: number, supplierId: number): Promise<number> {
    const [result] = await this.pool.execute(
      `INSERT INTO crm_user_supplier_pool (user_id, supplier_id, source)
       VALUES (?, ?, 'manual')`,
      [userId, supplierId],
    );
    return Number((result as any).insertId ?? 0);
  }

  /** 更新备注 */
  async updateNotes(userId: number, poolId: number, notes: string): Promise<void> {
    await this.pool.execute(
      "UPDATE crm_user_supplier_pool SET notes = ? WHERE id = ? AND user_id = ?",
      [notes, poolId, userId],
    );
  }

  /** 删除记录 */
  async remove(userId: number, poolId: number): Promise<void> {
    await this.pool.execute(
      "DELETE FROM crm_user_supplier_pool WHERE id = ? AND user_id = ?",
      [poolId, userId],
    );
  }

  /** 统计用户资源库数量 */
  async countByUser(userId: number): Promise<number> {
    const [rows] = await this.pool.query(
      "SELECT COUNT(*) AS cnt FROM crm_user_supplier_pool WHERE user_id = ?",
      [userId],
    );
    return Number((rows as RowDataPacket[])[0]?.cnt ?? 0);
  }

  /** 回写诊断记录关联 */
  async linkQualification(userId: number, poolId: number, qualificationId: number): Promise<void> {
    await this.pool.execute(
      "UPDATE crm_user_supplier_pool SET qualification_id = ? WHERE id = ? AND user_id = ?",
      [qualificationId, poolId, userId],
    );
  }

  /** 获取资源库中供应商的完整画像（用于 AI 匹配） */
  async fetchSupplierProfiles(userId: number): Promise<Record<string, unknown>[]> {
    const [rows] = await this.pool.query(
      `SELECT p.id AS pool_id, s.id AS supplier_id,
              s.company, s.industry, s.products, s.certification,
              s.country, s.city, s.type, s.registered_capital, s.established_at, s.intro,
              q.employee_count, q.export_scale, q.service_countries,
              q.overseas_companies, q.ungm_status, q.english_team,
              q.payment_terms, q.bid_willingness
       FROM crm_user_supplier_pool p
       INNER JOIN supplier s ON s.id = p.supplier_id
       LEFT JOIN crm_supplier_qualification q ON q.id = p.qualification_id
       WHERE p.user_id = ? AND p.supplier_id IS NOT NULL`,
      [userId],
    );
    return rows as Record<string, unknown>[];
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/unit/repos/user-supplier-pool.repo.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/lib/repos/user-supplier-pool.repo.ts tests/unit/repos/user-supplier-pool.repo.test.ts
git commit -m "feat: 新增用户供应商资源库数据访问层"
```

---

## Task 3: 供应商资源库 API（CRUD）

**Files:**
- Create: `src/app/api/user/supplier-pool/route.ts`
- Create: `src/app/api/user/supplier-pool/[id]/route.ts`

- [ ] **Step 1: 创建列表 + 添加 API**

```typescript
// src/app/api/user/supplier-pool/route.ts
/**
 * GET /api/user/supplier-pool — 列出我的供应商资源库
 * POST /api/user/supplier-pool — 添加供应商
 */
import { NextResponse } from "next/server";
import { z } from "zod";
import { getContext } from "@/lib/db/context";
import { requireUserKeyOrThrow } from "@/lib/middleware/auth";
import { withRoute, routeError, parseJson } from "@/lib/middleware/route-handler";
import { EC_INVALID_PARAMS } from "@/shared/constants/api";
import { UserSupplierPoolRepo } from "@/lib/repos/user-supplier-pool.repo";
import { SupplierDirectoryRepo } from "@/lib/repos/suppliers/supplier-directory.repo";
import { SupplierQualificationRepo } from "@/lib/repos/supplier-qualification.repo";
import type { Pool, RowDataPacket } from "mysql2/promise";

const MAX_POOL_SIZE = 50;

/** GET: 列出我的供应商资源库 */
export const GET = withRoute(async (req: Request) => {
  const auth = await requireUserKeyOrThrow(req);
  const ctx = getContext();
  const repo = new UserSupplierPoolRepo(ctx.dbPool);
  const items = await repo.listByUser(auth.userId);
  return NextResponse.json({ code: 0, message: "ok", data: { list: items } });
});

const addBodySchema = z.object({
  companyName: z.string().min(1).max(200),
});

/** POST: 添加供应商（输入公司名称 → 搜索平台目录 → 匹配或创建） */
export const POST = withRoute(async (req: Request) => {
  const auth = await requireUserKeyOrThrow(req);
  const body = await parseJson(req, addBodySchema);
  const ctx = getContext();
  const poolRepo = new UserSupplierPoolRepo(ctx.dbPool);

  // 检查上限
  const count = await poolRepo.countByUser(auth.userId);
  if (count >= MAX_POOL_SIZE) {
    routeError(400, 40020, "资源库已满（上限 50 个），请移除不用的供应商后再添加");
  }

  const companyName = body.companyName.trim();

  // 搜索平台供应商目录
  const dirRepo = new SupplierDirectoryRepo(ctx.dbPool);
  const matched = await findSupplierByCompany(ctx.dbPool, companyName);

  if (matched) {
    // 匹配到平台供应商
    const qualId = await findQualificationId(ctx.dbPool, matched.id);
    const poolId = await poolRepo.addFromPlatform(auth.userId, matched.id, qualId);
    if (!poolId) {
      routeError(400, 40021, "该供应商已在你的资源库中");
    }
    return NextResponse.json({ code: 0, message: "ok", data: { poolId, source: "platform", supplierId: matched.id } });
  }

  // 未匹配到：创建 supplier 记录（仅 company 字段，verify_status='pending' 不进入公共目录）
  const [insertResult] = await ctx.dbPool.execute(
    "INSERT INTO supplier (company, verify_status, created_at) VALUES (?, 'pending', NOW())",
    [companyName],
  );
  const newSupplierId = Number((insertResult as any).insertId);
  const poolId = await poolRepo.addManual(auth.userId, newSupplierId);
  return NextResponse.json({ code: 0, message: "ok", data: { poolId, source: "manual", supplierId: newSupplierId } });
});

/** 按公司名搜索平台供应商（精确 + 模糊） */
async function findSupplierByCompany(pool: Pool, company: string) {
  // 先精确匹配
  const [exact] = await pool.query(
    `SELECT id, company, industry FROM supplier
     WHERE company = ? AND (verify_status = 'done' OR verify_status IS NULL)
     LIMIT 1`,
    [company],
  );
  if ((exact as RowDataPacket[]).length > 0) return (exact as RowDataPacket[])[0];
  // 再模糊匹配
  const [fuzzy] = await pool.query(
    `SELECT id, company, industry FROM supplier
     WHERE company LIKE ? AND (verify_status = 'done' OR verify_status IS NULL)
     ORDER BY CHAR_LENGTH(company) ASC LIMIT 1`,
    [`%${company}%`],
  );
  return (fuzzy as RowDataPacket[]).length > 0 ? (fuzzy as RowDataPacket[])[0] : null;
}

/** 查找供应商关联的最新诊断记录 ID */
async function findQualificationId(pool: Pool, supplierId: number): Promise<number | null> {
  const [rows] = await pool.query(
    `SELECT q.id FROM crm_supplier_qualification q
     INNER JOIN crm_users u ON u.id = q.user_id
     WHERE u.supplier_id = ?
     ORDER BY q.id DESC LIMIT 1`,
    [supplierId],
  );
  return (rows as RowDataPacket[])[0]?.id ?? null;
}
```

- [ ] **Step 2: 创建单条编辑/删除 API**

```typescript
// src/app/api/user/supplier-pool/[id]/route.ts
/**
 * PATCH /api/user/supplier-pool/:id — 编辑备注
 * DELETE /api/user/supplier-pool/:id — 移除供应商
 */
import { NextResponse } from "next/server";
import { z } from "zod";
import { getContext } from "@/lib/db/context";
import { requireUserKeyOrThrow } from "@/lib/middleware/auth";
import { withRoute, routeError, parseJson } from "@/lib/middleware/route-handler";
import { EC_INVALID_PARAMS } from "@/shared/constants/api";
import { UserSupplierPoolRepo } from "@/lib/repos/user-supplier-pool.repo";

const patchBodySchema = z.object({
  notes: z.string().max(500),
});

/** PATCH: 编辑备注 */
export const PATCH = withRoute<{ params: Promise<{ id: string }> }>(
  async (req, { params }) => {
    const auth = await requireUserKeyOrThrow(req);
    const { id } = await params;
    const poolId = Number(id);
    if (!Number.isFinite(poolId) || poolId <= 0) {
      routeError(400, EC_INVALID_PARAMS, "无效的资源库记录 ID");
    }
    const body = await parseJson(req, patchBodySchema);
    const ctx = getContext();
    const repo = new UserSupplierPoolRepo(ctx.dbPool);
    await repo.updateNotes(auth.userId, poolId, body.notes);
    return NextResponse.json({ code: 0, message: "ok" });
  },
);

/** DELETE: 移除供应商 */
export const DELETE = withRoute<{ params: Promise<{ id: string }> }>(
  async (req, { params }) => {
    const auth = await requireUserKeyOrThrow(req);
    const { id } = await params;
    const poolId = Number(id);
    if (!Number.isFinite(poolId) || poolId <= 0) {
      routeError(400, EC_INVALID_PARAMS, "无效的资源库记录 ID");
    }
    const ctx = getContext();
    const repo = new UserSupplierPoolRepo(ctx.dbPool);
    await repo.remove(auth.userId, poolId);
    return NextResponse.json({ code: 0, message: "ok" });
  },
);
```

- [ ] **Step 3: 提交**

```bash
git add src/app/api/user/supplier-pool/
git commit -m "feat: 新增供应商资源库 CRUD API"
```

---

## Task 4: AI 智能匹配服务

**Files:**
- Create: `src/lib/services/ai-match/index.ts`
- Create: `src/app/api/notices/[id]/ai-match/route.ts`

- [ ] **Step 1: 实现 AI 匹配服务**

```typescript
// src/lib/services/ai-match/index.ts
/**
 * AI 智能匹配服务
 * @module lib/services/ai-match
 * @description 从用户供应商资源库中推荐 Top N 最匹配公告的供应商。
 *              复用 ai-score 的 prompt 模板和 LLM 调用链。
 */
import type { Pool, RowDataPacket } from "mysql2/promise";
import { LlmConfigRepo } from "../../repos/llm-config.repo";
import { AiSummaryRepo } from "../../repos/ai-summary.repo";
import { UserSupplierPoolRepo } from "../../repos/user-supplier-pool.repo";
import { decryptApiKey } from "../ai-summary/crypto";
import { callLlmForScore } from "../ai-score/llm-client";
import { SCORE_SYSTEM_PROMPT, buildScoreUserPrompt, type AiScoreRaw } from "../ai-score/prompt";
import { errLlmNotConfigured, errNoticeNotFound, errLlmCallFailed } from "../ai-summary/errors";

export interface MatchedSupplier extends AiScoreRaw {
  pool_id: number;
  supplier_id: number;
  company: string;
}

export interface AiMatchResult {
  top: MatchedSupplier[];
  cached: boolean;
}

/** 获取公告基础信息（复用 ai-score 逻辑） */
async function fetchNotice(pool: Pool, noticeId: number) {
  const [rows] = await pool.query(
    `SELECT n.id, n.title, n.notice_type, n.country, n.deadline, n.estimated_value
     FROM crm_bid_notices n WHERE n.id = ? LIMIT 1`,
    [noticeId],
  );
  const base = (rows as RowDataPacket[])[0];
  if (!base) return null;
  const [oppRows] = await pool.query(
    `SELECT o.eligibility, o.technical_hurdles, o.supplier_conditions
     FROM crm_bid_opportunities o
     WHERE o.source_notice_id = (SELECT notice_id FROM crm_bid_notices WHERE id = ? LIMIT 1)
       AND (o.is_qualified = 1 OR o.status = 1 OR o.audit_status = 1)
     LIMIT 1`,
    [noticeId],
  );
  const opp = (oppRows as RowDataPacket[])[0];
  return { ...base, eligibility: opp?.eligibility || "", technical_hurdles: opp?.technical_hurdles || "", supplier_conditions: opp?.supplier_conditions || "" };
}

const TOP_N = 3;
const PRE_FILTER_LIMIT = 5;

/** 主入口：AI 智能匹配 */
export async function getOrGenerateAiMatch(
  pool: Pool,
  userId: number,
  noticeId: number,
  forceRegenerate = false,
): Promise<AiMatchResult> {
  const summaryRepo = new AiSummaryRepo(pool);
  const configRepo = new LlmConfigRepo(pool);
  const poolRepo = new UserSupplierPoolRepo(pool);

  // 检查缓存（match_results 字段）
  if (!forceRegenerate) {
    const cached = await summaryRepo.findScore(userId, noticeId);
    if (cached?.match_results) {
      return { top: JSON.parse(cached.match_results), cached: true };
    }
  }

  // 获取资源库供应商画像
  const suppliers = await poolRepo.fetchSupplierProfiles(userId);
  if (suppliers.length === 0) {
    return { top: [], cached: false };
  }

  // 获取公告
  const notice = await fetchNotice(pool, noticeId);
  if (!notice) errNoticeNotFound();

  // 获取 LLM 配置
  const config = await configRepo.findActiveByUser(userId);
  if (!config) errLlmNotConfigured();

  let apiKey: string;
  try { apiKey = decryptApiKey(config!.api_key); } catch { errLlmNotConfigured(); }

  // 当供应商 > 5 时粗筛（取前 N 个，后续迭代可加入 UNSPSC 行业匹配度粗筛）
  let candidates = suppliers;
  if (candidates.length > PRE_FILTER_LIMIT) {
    candidates = candidates.slice(0, PRE_FILTER_LIMIT);
  }

  // 批量评分
  const scored: MatchedSupplier[] = [];
  for (const supplier of candidates) {
    const userPrompt = buildScoreUserPrompt(notice as any, supplier);
    try {
      const result = await callLlmForScore(
        { baseUrl: config!.base_url, apiKey, model: config!.model },
        SCORE_SYSTEM_PROMPT,
        userPrompt,
      );
      scored.push({
        ...result.data,
        pool_id: Number(supplier.pool_id),
        supplier_id: Number(supplier.supplier_id),
        company: String(supplier.company || ""),
      });
    } catch {
      // 单个供应商评分失败不阻塞其他
    }
  }

  // 排序取 Top N
  scored.sort((a, b) => b.overall - a.overall);
  const top = scored.slice(0, TOP_N);

  // 写入缓存（match_results 字段）
  if (top.length > 0) {
    await summaryRepo.upsertScore({
      userId, noticeId,
      qualification: top[0].qualification,
      experience: top[0].experience,
      certification: top[0].certification,
      region: top[0].region,
      scale: top[0].scale,
      delivery: top[0].delivery,
      price: top[0].price,
      overall: top[0].overall,
      reasons: JSON.stringify(top.map((s) => ({
        pool_id: s.pool_id, supplier_id: s.supplier_id, company: s.company,
        overall: s.overall, details: s.details,
      }))),
      model: config!.model,
      providerBaseUrl: config!.base_url,
    });
  }

  return { top, cached: false };
}
```

- [ ] **Step 2: 创建 API 端点**

```typescript
// src/app/api/notices/[id]/ai-match/route.ts
/**
 * POST /api/notices/:id/ai-match — AI 智能匹配
 * GET /api/notices/:id/ai-match — 获取匹配结果缓存
 */
import { NextResponse } from "next/server";
import { z } from "zod";
import { getContext } from "@/lib/db/context";
import { requireUserKeyOrThrow } from "@/lib/middleware/auth";
import { withRoute, routeError, parseJson } from "@/lib/middleware/route-handler";
import { EC_INVALID_PARAMS } from "@/shared/constants/api";
import { getOrGenerateAiMatch } from "@/lib/services/ai-match";

const bodySchema = z.object({
  forceRegenerate: z.boolean().optional().default(false),
});

export const POST = withRoute<{ params: Promise<{ id: string }> }>(
  async (req, { params }) => {
    const auth = await requireUserKeyOrThrow(req);
    const { id } = await params;
    const noticeId = Number(id);
    if (!Number.isFinite(noticeId) || noticeId <= 0) {
      routeError(400, EC_INVALID_PARAMS, "无效的公告 ID");
    }
    // 解锁校验
    const ctx = getContext();
    const unlock = await ctx.notice.unlockRepo.findUnlock(auth.userId, noticeId);
    if (!unlock) routeError(403, 40013, "公告已锁定，请先解锁", { core_locked: true });

    const body = await parseJson(req, bodySchema);
    const result = await getOrGenerateAiMatch(ctx.dbPool, auth.userId, noticeId, body.forceRegenerate);
    return NextResponse.json({ code: 0, message: "ok", data: result });
  },
);
```

- [ ] **Step 3: 提交**

```bash
git add src/lib/services/ai-match/ src/app/api/notices/[id]/ai-match/
git commit -m "feat: 新增 AI 智能匹配服务与 API"
```

---

## Task 5: 前端 API 客户端 + Hook

**Files:**
- Create: `src/features/procurement/api/ai-match.ts`
- Create: `src/features/procurement/hooks/useAiMatch.ts`

- [ ] **Step 1: 创建 API 客户端**

```typescript
// src/features/procurement/api/ai-match.ts
/**
 * AI 智能匹配前端 API
 * @module features/procurement/api/ai-match
 */
import { getAuthToken } from "@/core/http";
import type { DimensionDetail } from "./ai-score";

export interface MatchedSupplier {
  pool_id: number;
  supplier_id: number;
  company: string;
  qualification: number;
  experience: number;
  certification: number;
  region: number;
  scale: number;
  delivery: number;
  price: number;
  overall: number;
  details: Record<string, DimensionDetail>;
  reasoning: string;
}

export interface AiMatchData {
  top: MatchedSupplier[];
  cached: boolean;
}

/** 触发 AI 智能匹配 */
export async function fetchAiMatch(
  noticeId: number,
  forceRegenerate = false,
): Promise<AiMatchData> {
  const authToken = getAuthToken();
  const headers: Record<string, string> = { "Content-Type": "application/json" };
  if (authToken) headers["Authorization"] = `Bearer ${authToken}`;

  const res = await fetch(`/api/notices/${noticeId}/ai-match`, {
    method: "POST",
    headers,
    credentials: "same-origin",
    body: JSON.stringify({ forceRegenerate }),
  });

  if (!res.ok) throw new Error(`HTTP ${res.status}`);
  const json = await res.json();
  return json.data as AiMatchData;
}
```

- [ ] **Step 2: 创建 Hook**

```typescript
// src/features/procurement/hooks/useAiMatch.ts
/**
 * AI 智能匹配数据 Hook
 * @module features/procurement/hooks/useAiMatch
 */
import { useCallback, useState } from "react";
import { fetchAiMatch, type AiMatchData } from "../api/ai-match";

export interface UseAiMatchReturn {
  data: AiMatchData | null;
  loading: boolean;
  error: string | null;
  triggerMatch: (forceRegenerate?: boolean) => void;
}

export function useAiMatch(noticeId: number | undefined): UseAiMatchReturn {
  const [data, setData] = useState<AiMatchData | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const triggerMatch = useCallback((force = false) => {
    if (!noticeId) return;
    setLoading(true);
    setError(null);
    fetchAiMatch(noticeId, force)
      .then((d) => setData(d))
      .catch((err) => setError(err instanceof Error ? err.message : "匹配失败"))
      .finally(() => setLoading(false));
  }, [noticeId]);

  return { data, loading, error, triggerMatch };
}
```

- [ ] **Step 3: 提交**

```bash
git add src/features/procurement/api/ai-match.ts src/features/procurement/hooks/useAiMatch.ts
git commit -m "feat: 新增 AI 智能匹配前端 API 客户端与 Hook"
```

---

## Task 6: AiMatchCard 组件

**Files:**
- Create: `src/features/procurement/components/AiMatchCard.tsx`

- [ ] **Step 1: 实现组件**

组件结构参照现有 `AiScoreCard`，但展示 Top 3 供应商排行：

- 未触发状态：引导按钮"AI 智能匹配"
- 加载中：骨架屏
- 空结果（资源库为空）：引导"去建立资源库"
- 有结果：Top 3 供应商卡片列表
  - 每个卡片：排名 + 公司名 + 综合分 + 等级标签
  - 核心优势（matched）+ 主要短板（gaps）
  - 可展开查看 7 维度进度条 + 评判标准 + 评分依据
  - 底部"重新匹配"按钮

组件 props 接口：
```typescript
export interface AiMatchCardProps {
  data: AiMatchData | null;
  loading: boolean;
  error: string | null;
  onStart: () => void;
  onRegenerate: () => void;
  onGoToPool: () => void; // 跳转资源库管理页
}
```

视觉风格与 AiScoreCard 保持一致（紫色主题、圆角卡片、进度条着色规则 ≥70 绿 / 40-69 黄 / <40 红）。

- [ ] **Step 2: 提交**

```bash
git add src/features/procurement/components/AiMatchCard.tsx
git commit -m "feat: 新增 AI 智能匹配结果卡片组件"
```

---

## Task 7: 供应商资源库管理页

**Files:**
- Create: `src/app/(public)/settings/supplier-pool/page.tsx`
- Create: `src/app/(public)/settings/supplier-pool/page-client.tsx`

- [ ] **Step 1: 创建 Shell 页面**

```typescript
// src/app/(public)/settings/supplier-pool/page.tsx
import type { Metadata } from "next";
import SupplierPoolPageClient from "./page-client";

export const metadata: Metadata = { title: "供应商资源库" };

export default function SupplierPoolPage() {
  return <SupplierPoolPageClient />;
}
```

- [ ] **Step 2: 创建 Client 页面**

功能清单：
1. 列表展示资源库（公司名、行业、诊断状态标签、备注）
2. "添加合作工厂"按钮 → 输入公司名称 → 调用 POST /api/user/supplier-pool
3. 添加成功后弹窗引导填写诊断表（复用 `QualificationFormFields`）
4. 每行操作：编辑备注（内联编辑）、填写/查看诊断表、移除
5. 诊断状态标签：已完善（绿色）/ 待完善（橙色 + 引导标记）

API 调用使用 `fetch` 直接调用 `/api/user/supplier-pool` 端点。

- [ ] **Step 3: 提交**

```bash
git add src/app/\(public\)/settings/supplier-pool/
git commit -m "feat: 新增供应商资源库管理页"
```

---

## Task 8: 设置中心导航 + i18n

**Files:**
- Modify: `src/app/(public)/settings/layout.tsx`
- Modify: `src/core/i18n/locales/zh/common.json`
- Modify: `src/core/i18n/locales/en/common.json`
- Modify: `src/core/i18n/locales/fr/common.json`
- Modify: `src/core/i18n/locales/ru/common.json`
- Modify: `src/core/i18n/locales/es/common.json`
- Modify: `src/core/i18n/locales/ar/common.json`

- [ ] **Step 1: 添加导航项**

在 `layout.tsx` 的 `NAV_ITEMS` 数组中，在"企业信息"之后添加：

```typescript
import { Cpu, User, Building2, Package } from "lucide-react";

const NAV_ITEMS = [
  { href: "/settings/profile", labelKey: "settingsProfile", icon: User },
  { href: "/settings/enterprise", labelKey: "settingsEnterprise", icon: Building2 },
  { href: "/settings/supplier-pool", labelKey: "settingsSupplierPool", icon: Package },
  { href: "/settings/ai-model", labelKey: "settingsAiModel", icon: Cpu },
];
```

- [ ] **Step 2: 添加 i18n key**

需要新增的 key（以中文为例）：

```json
{
  "settingsSupplierPool": "供应商资源库",
  "supplierPoolTitle": "供应商资源库",
  "supplierPoolAdd": "添加合作工厂",
  "supplierPoolCompanyName": "公司名称",
  "supplierPoolIndustry": "行业",
  "supplierPoolDiagStatus": "诊断状态",
  "supplierPoolDiagDone": "已完善",
  "supplierPoolDiagPending": "待完善",
  "supplierPoolNotes": "备注",
  "supplierPoolActions": "操作",
  "supplierPoolEmpty": "你还没有添加合作工厂",
  "supplierPoolEmptyHint": "输入公司名称即可添加，完善诊断信息可提升 AI 匹配准确度",
  "supplierPoolRemove": "移除",
  "supplierPoolEditNotes": "编辑备注",
  "supplierPoolFillDiag": "填写诊断表",
  "supplierPoolViewDiag": "查看诊断",
  "supplierPoolDiagGuide": "完善诊断信息可提升匹配准确度",
  "supplierPoolFull": "资源库已满（上限 50 个）",
  "supplierPoolDuplicate": "该供应商已在资源库中",
  "aiMatchTitle": "AI 智能匹配",
  "aiMatchIntro": "AI 将从你的供应商资源库中推荐最合适的工厂",
  "aiMatchStart": "开始匹配",
  "aiMatchRegenerate": "重新匹配",
  "aiMatchEmpty": "你还没有添加合作工厂，去建立资源库",
  "aiMatchGoToPool": "去建立资源库",
  "aiMatchRank": "推荐排名",
  "aiMatchOverall": "综合适配分",
  "aiMatchStrengths": "核心优势",
  "aiMatchGaps": "主要短板",
  "aiMatchDisclaimer": "匹配结果由 AI 基于公告要求与供应商画像自动生成，仅供参考。"
}
```

其他 5 种语言提供对应翻译。

- [ ] **Step 3: 提交**

```bash
git add src/app/\(public\)/settings/layout.tsx src/core/i18n/locales/
git commit -m "feat: 设置中心添加供应商资源库导航 + 6 语言 i18n"
```

---

## Task 9: 公告详情页集成（用户分流）

**Files:**
- Modify: 公告详情页中渲染 AiScoreCard 的位置

- [ ] **Step 1: 添加用户分流逻辑**

在公告详情页的 AI 评分 Tab 中，根据 `supplier_id` 分流：

```typescript
// 伪代码
import { AiScoreCard } from "@/features/procurement/components/AiScoreCard";
import { AiMatchCard } from "@/features/procurement/components/AiMatchCard";
import { useAiScore } from "@/features/procurement/hooks/useAiScore";
import { useAiMatch } from "@/features/procurement/hooks/useAiMatch";

// 获取用户绑定状态
const hasSupplier = !!user?.supplier_id;

if (hasSupplier) {
  // 企业用户：现有单供应商评分
  const scoreResult = useAiScore(noticeId);
  return <AiScoreCard {...scoreResult} />;
} else {
  // 个人用户：多供应商智能匹配
  const matchResult = useAiMatch(noticeId);
  return <AiMatchCard {...matchResult} onGoToPool={() => router.push("/settings/supplier-pool")} />;
}
```

- [ ] **Step 2: 提交**

```bash
git add src/features/procurement/
git commit -m "feat: 公告详情页 AI 评分 Tab 用户分流（企业/个人）"
```

---

## Task 10: TypeScript 编译 + 测试验证

- [ ] **Step 1: 编译检查**

```bash
npx tsc --noEmit
```

Expected: 0 errors

- [ ] **Step 2: 运行全量测试**

```bash
npx vitest run
```

Expected: 所有测试通过

- [ ] **Step 3: 最终提交**

```bash
git add -A
git commit -m "feat: 外贸员供应商资源库 + AI 智能匹配 完整交付"
```
