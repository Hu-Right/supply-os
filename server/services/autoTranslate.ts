/**
 * 增量双语翻译定时任务
 * 已迁移至 translation/auto.ts，本文件为向后兼容的 barrel re-export。
 * @see translation/auto.ts
 */
export { startAutoTranslate, runIncrementalTranslation } from "./translation/auto";
export type { AutoTranslateConfig } from "./translation/auto";
