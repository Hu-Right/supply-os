# 支付弹窗交互问题修复总结

## 修复的问题

### 问题 1：支付弹窗误触关闭
**现象**：在支付弹窗打开状态下，用户点击遮罩层、按 ESC 键或拖拽弹窗时，弹窗会意外关闭，导致用户在扫码过程中误操作退出。

**解决方案**：
- 为 `Modal` 组件添加了三个新的可选属性：
  - `closeOnBackdrop?: boolean` - 是否允许点击遮罩层关闭（默认 true）
  - `closeOnEsc?: boolean` - 是否允许 ESC 键关闭（默认 true）
  - `closeOnDrag?: boolean` - 是否允许拖拽关闭（默认 true）
- 在 `PaymentModalCore` 中将这三个属性都设置为 `false`，确保支付弹窗只能通过右上角关闭按钮或支付完成后自动关闭

**修改文件**：
- `src/shared/ui/Modal.tsx` - 添加关闭行为控制属性
- `src/features/payment/components/PaymentModalCore.tsx` - 禁用所有非按钮关闭方式

### 问题 2：支付宝扫码后提示"即将离开支付宝"
**现象**：用户使用支付宝 App 扫描弹窗内的二维码后，系统提示"即将离开支付宝，点击继续访问"，用户体验断裂，无法直接完成支付。

**根本原因**：
之前的实现使用 `alipay.trade.page.pay`（电脑网站支付）接口，返回的是一个 HTML 表单，前端将其跳转端点 URL 编码为二维码。当支付宝 App 扫描这个二维码时，会打开一个非支付宝域名的网页，因此触发"即将离开支付宝"的安全提示。

**解决方案**：
改用 `alipay.trade.precreate`（当面付）接口生成支付宝原生二维码：
- `precreate` 接口返回的 `qr_code` 是支付宝原生支付链接（如 `https://qr.alipay.com/xxx`）
- 用户用支付宝 App 扫描后直接进入支付宝收银台，无需任何中间跳转
- 如果商户未开通当面付产品，自动回退到原有方案（使用跳转端点生成二维码）

**修改文件**：
- `server/payment/AlipayProvider.ts` - 在 `createPaymentUrl` 方法中优先调用 `precreate` 接口
- `server/routes/payment.routes.ts` - 优先使用支付渠道返回的原生二维码
- `server/services/training-payment.ts` - 同步更新研修班支付逻辑

## 技术实现细节

### 1. Modal 组件增强
```typescript
export interface ModalProps {
  // ... 原有属性
  closeOnBackdrop?: boolean;  // 新增
  closeOnEsc?: boolean;       // 新增
  closeOnDrag?: boolean;      // 新增
}
```

### 2. 支付宝双接口策略
```typescript
async createPaymentUrl(...) {
  // 1. 生成 page.pay 的 HTML 表单（用于跳转场景）
  const payUrl = this.sdk.pageExecute("alipay.trade.page.pay", options);
  
  // 2. 尝试使用 precreate 生成原生二维码（用于弹窗扫码场景）
  let qrCodeUrl: string | undefined;
  try {
    const precreateResult = await this.sdk.exec("alipay.trade.precreate", {
      bizContent: { out_trade_no, total_amount, subject }
    });
    if (precreateResult.code === "10000" && precreateResult.qr_code) {
      qrCodeUrl = precreateResult.qr_code;  // 原生支付宝二维码
    }
  } catch (err) {
    // 未开通当面付时静默回退
  }
  
  return { pay_url: payUrl, qr_code_url: qrCodeUrl };
}
```

### 3. 二维码生成优先级
```typescript
// 优先级：原生二维码 > 跳转端点二维码
if (result.qr_code_url) {
  // 支付渠道返回的原生二维码（如支付宝 precreate）
  qrCodeUrl = await toQrDataUrl(result.qr_code_url);
} else if (result.provider === "alipay") {
  // 回退：将跳转端点渲染为二维码
  qrCodeUrl = await toQrDataUrl(redirectUrl);
}
```

## 验证结果

✅ TypeScript 类型检查通过
✅ 345 个单元测试全部通过
✅ 向后兼容：未开通当面付的商户自动回退到原有方案

## 使用说明

### 对于商户
- 如果已开通支付宝"当面付"产品，弹窗支付将使用原生二维码，用户体验最佳
- 如果未开通"当面付"，自动回退到跳转端点方案，功能不受影响
- 开通当面付产品：登录支付宝商家中心 → 产品管理 → 申请开通"当面付"

### 对于用户
- 支付弹窗现在只能通过右上角 × 按钮或支付完成后关闭
- 点击遮罩层、按 ESC 键、拖拽弹窗都不会关闭
- 支付宝扫码后直接进入支付页面，无需额外确认

## 相关文件清单

### 前端
- `src/shared/ui/Modal.tsx` - Modal 组件（添加关闭行为控制）
- `src/features/payment/components/PaymentModalCore.tsx` - 统一支付弹窗核心

### 后端
- `server/payment/AlipayProvider.ts` - 支付宝支付策略（添加 precreate 支持）
- `server/routes/payment.routes.ts` - 会员支付路由
- `server/services/training-payment.ts` - 研修班支付服务

## 后续优化建议

1. **微信支付优化**：当前微信支付使用 Native 支付，已经是原生二维码，无需额外优化
2. **二维码有效期**：可以考虑在二维码下方显示剩余时间，提醒用户尽快支付
3. **支付超时提醒**：订单 30 分钟过期，可以在最后 5 分钟显示倒计时提醒
