# 采购公告详情页"锁定态闪烁"修复 实施记录

> 实施日期：2026-07-25
> 计划文档：`docs/superpowers/plans/2026-07-25-notice-detail-flicker-fix.md`
> 工作分支：`fix/notice-detail-flicker`（已合入 main，未推送远端）
> 执行方式：Inline TDD（红灯 → 实现 → 绿灯 → 提交，逐 Task 推进）

---

## 一、问题描述

### 1.1 现象

使用会员账号（或任何已解锁过公告的账号）访问一条**已经查看过/已解锁**的公告详情页时：

1. 页面先显示琥珀色"核心信息已隐藏"锁定面板（提示需解锁才能查看机构、联系方式等）；
2. 等待约 1~3 次网络往返后，锁定面板突然消失，替换为完整的解锁内容（机构全称、联系人、采购文件、来源链接）；
3. 每次重新打开同一条公告，上述"锁定 → 完整"的闪烁过程都会重演。

对已付费/已解锁用户而言，这个闪烁传递了错误信号（"我的解锁失效了？"），体验类似"付费内容被反复上锁"。

### 1.2 技术原因分析（三个根本原因）

**根因 ①：列表数据天生就是"锁定态"。**
后端 `/api/notices` 列表接口（server.ts L1915-1923）对**所有用户**硬编码返回 `core_locked: true, agency: null, ...`（锁定字段全部遮蔽），不区分该用户是否已解锁。前端 `openNotice` 的第一步是 `setSelectedNotice(notice)`——把列表里这条"天生锁定"的数据直接放进详情页渲染，于是**首帧必然是锁定面板**，无论用户实际解锁与否。

**根因 ②：串行请求链拉长了锁定态的停留时间。**
旧版 `openNotice` 的执行链是：
```
await viewNotice(...)          ← 第 1 次网络往返（浏览计数）
setSelectedNotice(notice)      ← 此刻才渲染详情页（锁定态首帧）
await refreshMembership()      ← 第 2 次网络往返（会员配额）
void loadNoticeDetail(notice)  ← 第 3 次网络往返（真正的解锁详情）
```
用户看到锁定面板的时间 ≈ 2~3 次网络往返的总和，闪烁被显著放大。

**根因 ③：详情请求零缓存，重复打开闪烁重演。**
`fetchNoticeDetail` 是裸 `fetch`，每次打开同一条公告都重新发起请求，闪烁在每次进入时都完整重演一遍，且浪费带宽。

### 1.3 根因层面的一个补充事实

前端**本地没有任何"哪些公告我已解锁"的状态**——解锁记录只存在后端 `crm_notice_unlocks` 表里，前端只有在详情请求成功返回后才"事后得知"该公告已解锁。这是根因 ① 无法在渲染层被规避的深层原因。

---

## 二、解决方案

### 2.1 方案选型

对比了五个候选方案（详见计划文档"方案取舍与影响评估"节），最终采用 **A + C + B 纯前端组合**，否决 D 与 E：

| 方案 | 内容 | 取舍 |
|---|---|---|
| A 并行化 | openNotice 三请求并行 | ✅ 采用：锁定态停留从 3 次往返缩至 1 次 |
| B 详情缓存 | fetchNoticeDetail 按 URL 缓存 Promise | ✅ 采用：重复打开秒开、零请求 |
| C 解锁集合 + 骨架屏 | 预取 unlocks 建 Set，已解锁公告首帧渲染骨架屏 | ✅ 采用（核心）：锁定面板首帧不再出现 |
| D 后端列表 per-user 锁定态 | 列表接口按用户返回真实 core_locked | ❌ 否决：需改 server.ts（远端主导文件，本地差异 +1）；列表 URL 带 user_key 会破坏 fetchJsonCached 缓存复用 |
| E 乐观渲染 | 假定已解锁直接渲染完整布局 | ❌ 否决：锁定内容（机构全称/联系人/文件）前端本无数据，乐观渲染只有空白 |

### 2.2 三层实现细节

#### 层 1（方案 B）：api 层——详情 Promise 会话缓存 + 解锁集合拉取

`src/features/procurement/api.ts`：

- `fetchNoticeDetail` 改为按完整 URL（含 noticeId + userKey）缓存 **Promise**（而非结果）：
  ```ts
  const noticeDetailCache = new Map<string, Promise<NoticeItem>>();
  // 命中直接返回同一 Promise（并发去重 + 结果复用）
  // 失败时 request.catch(() => noticeDetailCache.delete(url)) 自动清缓存
  ```
  - 缓存 Promise 而非结果：两次调用即使在首个请求未完成时并发发起，也只有一次网络请求；
  - 失败（403 未解锁）不缓存：解锁后重新调用会真实发起请求，数据自然刷新；
  - 键含 userKey：换账号不会串用户缓存。
- 新增 `fetchUnlockedNoticeIds(userKey)`：GET `/api/notices/unlocks?user_key=`（复用现存接口，**零后端改动**），把返回行映射为 `notice_id` 数组；任何异常（非 200、非数组、网络错误、脏数据 notice_id 为 null）一律返回 `[]`，永不抛错、永不阻断采购页。

#### 层 2（方案 C 前半）：NoticeDetail——`detailLoading` 骨架屏

`src/features/procurement/components/NoticeDetail.tsx`：

- 新增可选 prop `detailLoading?: boolean`；
- 派生 `showSkeleton = !coreUnlocked && !!detailLoading`——**已解锁数据到达后（core_locked === false）骨架屏自动让位**，即使父层还没复位加载态；
- 原"锁定琥珀面板 or 解锁内容"二分支扩为三分支：`coreUnlocked ? 完整内容 : showSkeleton ? 灰色骨架屏（data-testid="detail-skeleton"，animate-pulse 占位条） : 琥珀锁定面板`；
- 骨架屏期间机构名位置显示 `procurement_loading`（复用现有 i18n 键，零新增翻译）；
- "单条买断 ¥89"按钮在骨架屏期间隐藏（`!showSkeleton` 条件），避免向已解锁用户闪现付费按钮。

#### 层 3（方案 A + C 后半）：ProcurementPage——预取集合 + 并行化 + 免闪判定

`src/features/procurement/pages/ProcurementPage.tsx`：

- 新增状态 `unlockedIds: Set<number>`（已解锁公告集合）与 `detailLoadingId: number | null`（正在加载拓展详情的公告 id）；
- 登录后 useEffect 预取：`fetchUnlockedNoticeIds(userKey)` → `setUnlockedIds(new Set(ids))`；登出清空；带 `cancelled` 标志防卸载后 setState；
- `openNotice` 重写：
  ```
  alreadyUnlocked = unlockedIds.has(notice.id)
  已解锁 → 跳过免费上限检查（不触发付费墙）、不消耗免费次数、detailLoadingId = notice.id（骨架屏门控）
  viewNotice / refreshMembership / loadNoticeDetail 全部 void 并行，不再 await 串行
  ```
  锁定态（未解锁公告）行为完全不变：照常计数、照常显示锁定面板；
- `loadNoticeDetail`：成功后 `markUnlocked(notice.id)` 同步集合；`finally` 按 id 复位加载态（`prev === notice.id ? null : prev`）；
- 解锁/支付两条成功路径（`handleUnlockNotice`、`openNoticeById`）同步写入集合/加载态；`onBack` 兜底 `setDetailLoadingId(null)`；
- `<NoticeDetail detailLoading={detailLoadingId === selectedNotice.id} />`——按当前公告匹配传入。

### 2.3 修复后的用户旅程

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 打开已解锁公告（会话内第一次） | 锁定面板 2~3 次往返 → 完整内容 | 灰色骨架屏 1 次往返 → 完整内容 |
| 重复打开同一公告 | 闪烁重演 + 重复请求 | **秒开**（缓存命中，零请求零闪烁） |
| 刷新页面后打开已解锁公告 | 闪烁重演 | 骨架屏 →完整内容（unlockedIds 预取生效） |
| 打开未解锁公告 | 锁定面板 | 锁定面板（行为不变，回归确认） |
| 免费次数耗尽打开已解锁公告 | 弹付费墙（隐性 bug） | 直接进入详情（bug 修复） |

---

## 三、隐藏隐患（实施过程中识别的风险点与副作用）

### 隐患 1：测试用例 id 复用 × 新增缓存 = 用例互相污染
`procurement-detail-api.test.ts` 原有两个用例共用 `noticeId = 42`（一个成功、一个 403）。`fetchNoticeDetail` 加缓存后，模块级 `Map` 在同一测试文件内**跨用例存活**——第二个用例会命中第一个用例的成功缓存，403 分支永远不会被触发，测试假绿。

### 隐患 2：`vi.mock` 整体替换 api 模块，新增导出缺失即运行时炸裂
`ProcurementPage.test.tsx` 用 `vi.mock("@/features/procurement/api", () => ({...}))` **整体替换**模块。页面新 import 的 `fetchUnlockedNoticeIds` 若不补进 mock 工厂，所有旧用例会在渲染时抛 `fetchUnlockedNoticeIds is not a function`，14 个旧用例全灭。

### 隐患 3：全局布尔加载态在快速连点时的"错帧"竞态
若 `detailLoading` 用全局布尔：快速点开已解锁公告 A → 返回 → 点开已解锁公告 B，A 的请求先完成，其 `finally` 把布尔置 false，**B 还在加载却被撤掉骨架屏**，闪回锁定面板——闪烁以竞态形式回归。

### 隐患 4：`unlockedIds` 是新增状态源，漏写路径会导致集合失真
解锁状态的变化入口有四个：详情拉取成功、免费/配额解锁成功、单条买断支付成功、支付回跳。任何一条路径漏写 `markUnlocked`，该公告下次打开就会退回"锁定首帧"旧行为（功能不坏但闪烁复发）。

### 隐患 5：骨架屏卡死风险
`detailLoadingId` 若因异常路径（用户在请求完成前点返回、userKey 中途失效）未被复位，下次进入详情会永远停留在骨架屏。

### 隐患 6：行为变更的副作用（三项，皆为有意为之但需知晓）
1. **已解锁公告不再消耗免费次数、不再触发付费墙**——这同时修复了一个现存隐性 bug：free 用户花 ¥89 买断的公告，在免费浏览次数耗尽后自己都点不开（点开即弹付费墙）；
2. **`viewNotice` 浏览计数改为 fire-and-forget**——计数请求失败时前端无感知（后端计数行为本身不变）；
3. **详情会话级缓存**——同一会话内后端对公告详情的变更不可见（公告详情为静态爬虫数据，可接受）。

### 隐患 7：测试点击目标与真实 DOM 不符（实施中实际踩到）
计划中新用例写的是 `fireEvent.click(screen.getByText("Notice A"))`（点击标题文本）。实际 `NoticeCard` 的 `onClick` 绑定在"查看详情"**按钮**上，点击标题不会触发 `openNotice`，导致"锁定回归"用例红灯原因错误（详情页根本没打开，而非锁定面板缺失）。

---

## 四、隐患解决方案

| # | 隐患 | 解决方案 | 落点 |
|---|---|---|---|
| 1 | 缓存导致用例污染 | 每个用例使用**独立 noticeId**（42/43/44/45），并在文件头部留注释警示后来者："fetchNoticeDetail 现按 URL 缓存成功结果，各用例必须使用不同 noticeId" | `procurement-detail-api.test.ts` |
| 2 | mock 工厂缺新函数 | mock 工厂**同步补齐**两个新导出，且默认值等价旧行为（`fetchNoticeDetail` 默认 reject 403、`fetchUnlockedNoticeIds` 默认返回 `[]`）——旧用例语义零变化；`beforeEach` 重置默认值防用例间串扰 | `ProcurementPage.test.tsx` |
| 3 | 全局布尔竞态错帧 | 加载态升级为 `detailLoadingId: number \| null`（记录"正在为哪条公告加载"）：`finally` 只清自己的（`prev === notice.id ? null : prev`）；传给组件时按当前公告匹配 `detailLoading={detailLoadingId === selectedNotice.id}`——A 的完成不会误伤 B 的骨架屏 | `ProcurementPage.tsx` |
| 4 | 集合漏写路径 | 四条路径全部显式写入：①`loadNoticeDetail` 成功 → `markUnlocked`；②`handleUnlockNotice` 成功 → `markUnlocked`；③支付成功回调 `onPaid` → 走 `loadNoticeDetail` 间接写入；④支付回跳 `openNoticeById` → 同上。`markUnlocked` 用不可变 Set 更新保证 React 重渲染 | `ProcurementPage.tsx` |
| 5 | 骨架屏卡死 | 三重兜底：①`finally` 必然复位（成功/失败都走）；②`onBack` 显式 `setDetailLoadingId(null)`；③组件层 `showSkeleton = !coreUnlocked && !!detailLoading`——数据到达后骨架屏自动让位，即使加载态未复位也不会遮挡内容 | 组件 + 页面双层 |
| 6 | 行为变更副作用 | 三项变更均在计划文档"影响评估"节显式声明并经确认；其中变更 1 属 bug 修复（浏览器手工验证步骤 4 专项覆盖）；变更 3 的边界由"失败不缓存"保证——403 后解锁，重新拉取即得新数据 | 计划文档 + 手工验证 |
| 7 | 测试点击目标错误 | 改为点击 NoticeCard 的"查看详情"按钮：`fireEvent.click(screen.getAllByText("procurement_detail")[0])`（Notice A 首张卡）/ `[1]`（Notice B 第二张卡），并加注释说明点击语义 | `ProcurementPage.test.tsx` |

---

## 五、实施过程记录（Task 0-4）

### Task 0：建工作分支 ✅
```
git checkout main ; git checkout -b fix/notice-detail-flicker
→ Switched to a new branch 'fix/notice-detail-flicker'
```
前置检查：工作区干净（仅未跟踪的本记录文件），main 领先 origin/main 46 提交（既定状态，禁止推送）。

### Task 1：api 层——详情缓存 + 解锁集合拉取（TDD）✅
1. **红灯**：整体替换 `procurement-detail-api.test.ts`（6 用例，id 42/43/44/45 分离）→ 跑测试：`3 failed | 3 passed`——失败点精确符合预期（`fetchUnlockedNoticeIds` 不存在 ×2、缓存用例 hits=2）；
2. **实现**：`api.ts` 加 `noticeDetailCache`（Promise 缓存 + 失败清除）、新增 `fetchUnlockedNoticeIds`（异常回 `[]`）；
3. **绿灯**：`6 passed (6)`；
4. **提交**：`cc0d73e feat(procurement): cache notice detail + fetchUnlockedNoticeIds`。

### Task 2：NoticeDetail 骨架屏（TDD）✅
1. **红灯**：追加 2 用例 → `1 failed | 13 passed`（`detail-skeleton` 找不到；"ignores detailLoading once unlocked"用例因解锁分支天然忽略未知 prop 而先行通过，属预期）；
2. **实现**：四处修改——props 加 `detailLoading`、派生 `showSkeleton`、锁定面板二分支扩三分支（插入骨架屏）、买断按钮加 `!showSkeleton` 条件；
3. **绿灯**：`14 passed (14)`（12 旧 + 2 新）；
4. **提交**：`46f0752 feat(procurement): NoticeDetail skeleton state via detailLoading prop`。

### Task 3：ProcurementPage 集成（TDD）✅
1. **红灯（一波三折）**：按计划加 mock 工厂 2 函数 + beforeEach 默认值 + 2 新用例 → 首跑 `2 failed`，但"锁定回归"用例的失败原因不对（点击标题文本未触发 openNotice，详情页根本没打开）——**发现计划缺陷（隐患 7）**；
2. **修正测试**：点击目标改为"查看详情"按钮（`getAllByText("procurement_detail")[0]/[1]`）→ 重跑 `1 failed | 18 passed`——红灯精确化：仅免闪烁用例失败（旧行为先闪锁定面板），锁定回归用例通过（旧行为本就显示锁定面板）✔ 这才是正确的 TDD 红灯形态；
3. **实现**：七处修改——import 补 `fetchUnlockedNoticeIds`、新增 `unlockedIds`/`detailLoadingId` 状态 + `markUnlocked`、预取 effect、`loadNoticeDetail` 重写（markUnlocked + finally 按 id 复位）、`openNotice` 重写（alreadyUnlocked 判定 + 三请求并行）、`openNoticeById` 加载态、`handleUnlockNotice`/`onBack`/props 三处收尾；
4. **绿灯**：procurement 全模块 `6 文件 65 passed`；
5. **提交**：`0c3a98d fix(procurement): eliminate locked-panel flicker for unlocked notices`。

### Task 4：全量验证 + 合入 main ✅

**门禁**：
- `npm run typecheck` → 0 错误；
- `npm test` → **49 文件 409 用例全绿**（401 旧 + 8 新，与计划预期精确一致）。

**浏览器手工验证**（dev 服务 http://127.0.0.1:3039，账号 free@qq.com）：

| 步骤 | 场景 | 结果 |
|---|---|---|
| 1 | 登录并确认一条已解锁公告（"Business Rates and Surveying Services"） | ✅ |
| 2 | 返回列表再次打开该公告 | ✅ 骨架屏 → 完整内容（HM Revenue & Customs、联系方式、原始链接），**琥珀锁定面板全程未出现** |
| 3 | 打开未解锁公告（回归） | ✅ 琥珀锁定面板照常显示 + "付费解锁 ¥89"按钮照常出现 |
| 4 | F5 刷新后再次打开已解锁公告 | ✅ 首帧灰色骨架屏 → 完整内容，锁定面板未出现（unlockedIds 预取生效） |

**合入**：
```
git checkout main
git merge --no-ff fix/notice-detail-flicker
→ 48a1766 merge: 公告详情锁定态闪烁修复合入主线（6 文件，+212/-19）
git status 干净；未执行 git push（遵守禁推约束）
```

### 提交清单
| 提交 | 内容 |
|---|---|
| `cc0d73e` | feat(procurement): cache notice detail + fetchUnlockedNoticeIds |
| `46f0752` | feat(procurement): NoticeDetail skeleton state via detailLoading prop |
| `0c3a98d` | fix(procurement): eliminate locked-panel flicker for unlocked notices |
| `48a1766` | merge: 公告详情锁定态闪烁修复合入主线 |

---

## 六、技术挑战与解决策略汇总

1. **不能动后端**（server.ts 为远端主导文件）→ 全部修复收敛在前端三层，复用现存 `/api/notices/unlocks` 接口与 `procurement_loading` i18n 键，实现零后端改动、零新增 i18n 键；
2. **模块级缓存与测试隔离的冲突** → 用例 id 分离 + 文件头注释固化约定（隐患 1）；
3. **vi.mock 整体替换的脆弱性** → 新导出同步补工厂 + 默认值等价旧行为（隐患 2）；
4. **快速连点竞态** → 加载态从布尔升级为按公告 id 记录，finally 只清自己的（隐患 3）；这与"详情合并的 id 守卫"（`prev.id === notice.id`，代码原有）共同构成双层竞态防护；
5. **计划与真实 DOM 的偏差** → TDD 红灯阶段主动核对失败原因是否符合预期，发现点击目标错误后先修测试再实现，避免"假红灯"掩盖真实行为（隐患 7）；
6. **VIP 账号不可用**（远程库 vip@qq.com 无 active 订阅）→ 手工验证用"free 账号 + 已解锁公告"等价复现 VIP 场景，覆盖同一条代码路径（`unlockedIds` 判定不区分解锁来源）。

## 七、对现有系统的影响评估

- **改动范围**：6 个文件（3 源码 + 3 测试），全部位于 procurement 模块前端，**server.ts 零改动**（本地差异清单不新增条目）；
- **对未解锁用户**：行为完全不变（锁定面板、免费计数、付费墙逻辑原样保留，回归用例 + 手工步骤 3 双重确认）；
- **对已解锁用户**：免闪烁 + 秒开 + 免误计数/误付费墙（bug 修复）；
- **与翻译计划的衔接**：本修复先行合入。后续《公告采购说明按需翻译》计划（`2026-07-25-notice-on-demand-translation.md`）改动 NoticeDetail 的说明区块，与本次改动的锁定区块/aside 无行级冲突，按既定顺序接续执行即可。

---

## 八、合入后追加修复（人工验证反馈）

### 8.1 骨架屏契合原版布局

**反馈**：初版骨架屏只是"一小块灰框三条线"，与解锁后真实内容体量差距过大，数据到达时版面跳变明显。

**修复**（`NoticeDetail.tsx`）：骨架屏按解锁内容真实结构整版占位——标签区（标题条 + 6 个 chip 方块）、来源链接条、浅青色详情面板（teal-50/40 底 + teal-100 边框，内含三列元信息小卡、两张联系人横条卡、投标拆解列表框），配色与 `NoticeUnlockedDetails` 面板对齐。`data-testid="detail-skeleton"` 不变，既有用例零改动通过。

### 8.2 解锁路径漏传加载态（骨架屏不生效）

**现象**：未解锁公告点击"免费解锁/会员解锁"后，锁定面板持续显示直至详情数据完全返回，骨架屏从未出现。

**根因**：`detailLoadingId` 只在 `openNotice`（已解锁公告）和 `openNoticeById`（支付回跳）两处设置；`handleUnlockNotice`（解锁按钮路径）与 `onPaid`（同页支付回调）从未设置过——组件端 `showSkeleton = !coreUnlocked && !!detailLoading` 逻辑本身正确，但 prop 始终为 false。缓存机制无关：解锁前的 403 失败不缓存，解锁后是真实首次请求。

**修复**（`ProcurementPage.tsx`，TDD 红→绿）：
1. `handleUnlockNotice`：发起解锁请求前 `setDetailLoadingId(notice.id)`；`unlockNotice` 抛异常或返回 `!res.ok`（含 402 付费墙）时按 id 守卫复位 `prev === notice.id ? null : prev`，锁定面板恢复；成功路径由 `loadNoticeDetail` 的 `finally` 复位；
2. `onPaid`：同样前置 `setDetailLoadingId(noticeId)`，支付成功后的详情拉取期间以骨架屏过渡。

**新增用例**（`ProcurementPage.test.tsx`，mock 工厂将 `unlockNotice` 提升为可控的 `mockUnlockNotice`）：
- `shows skeleton during unlock until detail data arrives`：解锁成功但详情挂起期间骨架屏可见、锁定面板消失；详情返回后完整内容替换骨架屏；
- `restores the locked panel when unlock fails`：解锁失败（500）时骨架屏不残留、锁定面板恢复、失败提示可见。

**验证**：采购模块 6 文件 67 passed（65 旧 + 2 新）；全量 49 文件 411 passed；typecheck 0 错误。
