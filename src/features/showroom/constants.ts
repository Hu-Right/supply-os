/**
 * 展厅模块常量 — 静态数据本地入口
 * Showroom Constants — Local entry for static data
 *
 * @module features/showroom/constants
 * @description 将 @/data 的展厅数据通过 feature 内部入口重导出，
 *              保持 feature 自包含，不直接依赖全局 @/data 路径。
 */
export { EXHIBITION_HALLS } from "@/data/exhibition-halls";
