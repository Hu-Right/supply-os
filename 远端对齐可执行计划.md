# 远端对齐可执行计划（基于《远端版本业务UI差异对比报告》）

> 本计划为**可执行落地计划**，配套只读报告 `远端版本业务UI差异对比报告.md`。
>
> - **对齐基准（远端）**：`origin/main` = `8312f0c`（单体 `App.tsx` + `ProcurementNoticesPool.tsx` + `TrainingRegisterModal.tsx` 等）。
> - **实现分支（本地）**：`port/notice-payment`（`HEAD=5257907`），模块化架构 `features/ / shared/ / core/ / data/`。
> - **总原则**：**业务与 UI 全部向远端 `8312f0c` 对齐，但用当前本地的模块化 / 组件化结构实现**。远端逻辑抽为 feature 内组件 + hook，不回退到单体。
> - **约束**：仅改前端；`server.ts` 冻结（所需端点 `/api/leads/log`、`/api/payment/orders`、`/api/membership/plans` 等均已存在）；改动留在本分支，不提交、不推送。
> - **门禁**：每个任务完成后 `npx tsc --noEmit` + `npx vitest run` + `npx tsx scripts/check-i18n.ts` 全绿。

---

## 差异核对结论（2026-07-24 复核）

报告全部条目经与当前源码 + `git`（`origin/main=8312f0c`、分支 `port/notice-payment`）核对**属实**。其中 §4 有一处需修正认知：

- **§4 `TrainingRegisterModal` 实为远端孤儿文件**：远端 `App.tsx` 仅 `import TrainingPage`，**从未引用 `TrainingRegisterModal`**。因此"对齐远端"意味着该弹窗在远端运行态本就不存在入口，**非功能缺失**，降为最低优先级（见 P2-C）。

---

## 任务总览（按优先级执行）

| # | 优先级 | 任务 | 报告条目 | 类型 |
| --- | --- | --- | --- | --- |
| P0 | 最高·必修 | CRM 线索跟进录入落库 | §8.3 | 功能回退修复 |
| P1-A | 高 | 采购详情内嵌多套餐付费面板恢复 | §1.1 §1.5 | 业务/UI 对齐 |
| P1-B | 高 | "核心信息已隐藏"遮罩门控 | §1.3 | UI 对齐 |
| P1-C | 高 | "投标拆解建议"卡片恢复 | §1.2 | UI 对齐 |
| P1-D | 中 | 交付卡 `notice.url` 原始链接 + `key_contacts` 兼容 | §1.4 | UI 对齐 |
| P2-A | 中 | 记录面板概览数量徽标 / 首条预览 / 常驻刷新 | §3.1 | UI 对齐 |
| P2-B | 低 | 全局 `NoticeItem` 类型与 feature 内统一 | §7 | 技术债 |
| P2-C | 低 | `TrainingRegisterModal` 处理（孤儿文件） | §4 | 结论/可选 |
| P2-D | 低 | 新增 4 语言（fr/ru/es/ar）译文抽检 | §5 | 质量 |

---

## P0 —— CRM 线索跟进录入落库（必修回退）

**目标**：`FollowUpLogPanel` 的"录入至 CRM"由死按钮改为真正 `POST /api/leads/log` 并回写 `leads` 与当前线索时间线，成功后清空表单并提示（对齐远端 `addCrmFollowUpLog`）。

**远端依据**：`_origin_snapshot/App.tsx` L644-670——`POST /api/leads/log`（`{leadId, content, author:"运营经理 (${userEmail})", nextStatus}`），成功后 `setLeads(map 替换)` + `setActiveLeadForLog(updatedLead)` + 清空 + `alert(addLogSuccess)`。

**改动文件与实现**：
1. `src/features/crm/hooks/useCrmData.ts`
   - 新增导出 `addFollowUpLog(leadId: number, content: string, nextStatus?: string): Promise<Lead | null>`：`POST /api/leads/log`，成功后 `setLeads(prev => prev.map(l => l.id === updated.id ? updated : l))`，返回 `updated`；失败返回 `null`。
   - `author` 取当前登录用户邮箱（经 `useAuth()` 或 CrmPage 透传），无则回退 `"Operator"`。
   - 在 `UseCrmDataReturn` 类型中补 `addFollowUpLog`。
2. `src/features/crm/pages/CrmPage.tsx`：从 `useCrmData()` 解构 `addFollowUpLog`，作为 `onSubmitLog` 传入 `<LeadTracker>`。
3. `src/features/crm/components/LeadTracker.tsx`
   - props 增加 `onSubmitLog: (leadId, content, nextStatus?) => Promise<Lead | null>`。
   - 提交成功后用返回的 `updatedLead` **更新 `activeLead`**（使时间线即时回显）；透传 `onSubmit` 给 `FollowUpLogPanel`。
4. `src/features/crm/components/FollowUpLogPanel.tsx`
   - props 增加 `onSubmit`；`handleSubmit` 改为 `async`：校验非空 → `await onSubmit(lead.id, newEntry, status || lead.status)` → 成功则清空 `newEntry/status`；失败给出内联错误文案（不再仅清空）。

**i18n**：`addLogSuccess` 已存在（zh.json L56）。若新增失败提示键，需 6 语言同步。

**测试**：
- 扩展 `src/__tests__/features/crm/CrmComponents.test.tsx`：mock `onSubmit`，断言点击"录入至 CRM"触发 `onSubmit(leadId, content, status)`、成功后清空、失败保留输入。
- 扩展 `useCrmData` 测试（若无则新建）：MSW mock `POST /api/leads/log` 返回更新后的 lead，断言 `leads` 被回写。

---

## P1-A —— 采购详情内嵌多套餐付费面板恢复（§1.1 §1.5）

**目标**：在公告详情右侧 `<aside>` 恢复远端 `PaymentPanel`——支付方式切换（支付宝可用 / 微信禁用）、从 `/api/membership/plans` 拉取的**多套餐列表**逐条渲染价格 + 购买、订单创建、mock"确认完成"、真实支付跳转、3 秒轮询对账。用模块化组件 + hook 实现，**不回退单体**。同时恢复 §1.5：免费额度用尽时 `openNotice` / `unlockNotice(402)` 打开公告 **并弹出付费面板**。

**远端依据**：`_origin_snapshot/ProcurementNoticesPool.tsx`——`PaymentPanel`（L1106-1234）、`paidPlans` 拉取（L407-409）、`createPaymentOrder`（L622-667）、`startPaymentPolling`（L669-704）、`markPaymentPaid`（L706-720）、`paywallNotice` 触发点（`openNotice` L522-529、`unlockNotice` 402 L575-583、`expressInterest` L615）。

**改动文件与实现**：
1. **新建** `src/features/procurement/components/NoticePaymentPanel.tsx`：远端 `PaymentPanel` 的模块化版。props：`{ plans, provider, order, busyPlanCode, message, onProviderChange, onCreateOrder, onMockPaid, onClose }`。文案改用 `useLocale().t()`；金额用现有 `formatMoney`（或 payment 内工具）。
2. **新建** `src/features/procurement/hooks/useNoticePayment.ts`：集中付费状态 `paywallNotice / paymentOrder / paymentProvider / busyPlanCode / paymentMessage / paidPlans`，暴露 `openPaywall(notice)`、`createOrder(planCode)`、`markPaid()`、`closePaywall()`、`startPolling(orderNo, planCode, noticeId)`。内部复用 `features/payment/api.ts`（`createOrder`/`getOrderStatus`/`mockPaid`）与 `features/procurement/api.ts`（`fetchMembershipPlans`、`unlockNotice`、`fetchNoticeDetail`）。轮询实现同远端（3s 间隔、`paid` 时自动 `unlockNotice(single/subscription)` + 刷新，`closed/failed` 提示，80 次超时）。
3. `src/features/procurement/pages/ProcurementPage.tsx`
   - 接入 `useNoticePayment`；`fetchMembershipPlans()` 结果写入 `paidPlans`（当前 L125-127 拉取后丢弃，需改为存储）。
   - `openNotice`（L182-185）：免费上限时改为 `setSelectedNotice(notice)` + `openPaywall(notice)` + `setActionMessage(t("procurement_freeLimit"))`（对齐 §1.5）。
   - `handleUnlockNotice`（L289-292）：无配额时 `openPaywall(notice)`；`unlockNotice` 返回 402 时同样弹面板。
   - `handleExpressInterest` 成功后 `openPaywall(notice)`（对齐远端 L615）。
   - 详情分支：将 `<NoticeDetail>` 与 `<NoticePaymentPanel>`（`paywallNotice` 存在时）一并传入/渲染。
4. `src/features/procurement/components/NoticeDetail.tsx`：`<aside>` 内在配额卡下方条件渲染 `<NoticePaymentPanel>`（由 ProcurementPage 透传 paywall 相关 props 与回调）。**保留**现有"单条解锁 ¥89"按钮与"升级会员"入口不冲突（远端二者共存）。

**决策说明**：全局 `supply-os:pay` → App 级 `PaymentModal` 机制**保留**（学习/会员升级仍用它）；采购详情恢复其**专属内嵌多套餐面板**，与远端一致。

**i18n**：复用已迁移的 payment 键（`products/productsDesc/choosePay/orderNo/orderCreated/mockNote/mockPaid/creatingOrder/paymentTip/alipay/wechat/orderFail/paidOk/paidFail` 等）；缺失键按 6 语言补齐，`check-i18n` 通过。

**测试**：
- 新建 `src/__tests__/features/procurement/NoticePaymentPanel.test.tsx`：渲染多套餐、支付方式切换（微信禁用）、点击购买触发 `onCreateOrder(planCode)`、order 存在时 mock/真实支付按钮分支。
- 扩展 `ProcurementPage.test.tsx`：免费额度用尽时弹出付费面板（§1.5）；`fetchMembershipPlans` 结果渲染进面板。
- `useNoticePayment` hook 测试（MSW mock 订单创建 + 轮询 paid → 自动解锁）。

---

## P1-B —— "核心信息已隐藏"遮罩门控（§1.3）

**目标**：详情页按 `core_locked` 门控——未解锁（`core_locked !== false`）时隐藏 UNSPSC 标签与交付信息，改显示琥珀色"核心信息已隐藏"占位框，机构名显示为遮罩文案；解锁后（`core_locked === false`）才展示真实内容。

**远端依据**：`_origin_snapshot/ProcurementNoticesPool.tsx` L746-749（`coreUnlocked` / `visibleAgency`）、L801-897（`coreUnlocked ? 真实内容 : 琥珀遮罩框`）。

**改动文件与实现**：
1. `src/features/procurement/components/NoticeDetail.tsx`
   - 计算 `const coreUnlocked = notice.core_locked === false;`
   - 副标题/元信息网格的机构名用 `visibleAgency = coreUnlocked ? (agency_full||agency||organization||未知) : t("procurement_lockedCoreTitle")`。
   - UNSPSC 标签块（L97-109）、`<NoticeUnlockedDetails>`（L123）、`source_url` 链接（L111-121）改为 `coreUnlocked` 时渲染；否则渲染琥珀色遮罩框（`Lock` 图标 + `procurement_lockedCoreTitle` + `procurement_lockedCoreDesc`）。

**i18n（新增 6 语言）**：`procurement_lockedCoreTitle`（"核心信息已隐藏"）、`procurement_lockedCoreDesc`（"真实机构、联系方式、采购文件将在解锁后展示。"）。

**测试**：扩展 `NoticeDetail.test.tsx`：`core_locked:true` 时不渲染 UNSPSC/交付信息、渲染遮罩框、机构名为遮罩文案；`core_locked:false` 时渲染真实内容。

---

## P1-C —— "投标拆解建议"卡片恢复（§1.2）

**目标**：在已解锁交付信息区补回"投标拆解建议"卡片，4 条要点。

**远端依据**：`_origin_snapshot/ProcurementNoticesPool.tsx` L877-885。

**改动文件与实现**：
1. `src/features/procurement/components/NoticeUnlockedDetails.tsx`：在附件分组后新增"投标拆解建议"卡片：
   - 紧急度 `difficulty` / 注册门槛 `registration_level`；
   - 预算 `estimated_value` / 截止 `deadline`；
   - 分类代码：`unspsc_codes` 取前 4 个 `code`；
   - 下一步固定文案（下载 RFQ/附件、核对资质/交付/币种/签章）。
   - 缺字段用回退文案（"待评估/待确认/未公开/待补充"）。

**i18n（新增 6 语言）**：`procurement_bidBreakdownTitle` + 4 条要点模板键（含插值 `{difficulty}` 等）与回退词。

**测试**：扩展 `NoticeUnlockedDetails.test.tsx`：断言渲染标题与 4 条要点、分类代码取前 4、缺字段回退。

---

## P1-D —— 交付卡原始链接 + `key_contacts` 兼容（§1.4）

**目标**：交付卡内补回 `notice.url` "打开公告"入口；`key_contacts` 兼容**字符串**与**对象数组**两种形态（远端为纯文本字符串）。

**远端依据**：L827（`notice.url` → 打开公告）、L834（`key_contacts` 作纯文本渲染）。

**改动文件与实现**：
1. `src/features/procurement/components/NoticeUnlockedDetails.tsx`
   - 采购方信息处新增：`notice.url` 存在则渲染"打开公告"链接（`target=_blank`）。
   - `key_contacts`：若为 `string` 则整体作文本段渲染；若为数组则保持现有对象渲染逻辑。

**i18n（新增 6 语言）**：`procurement_originalLink`（"原始链接"）、`procurement_openNotice`（"打开公告"）。

**测试**：扩展 `NoticeUnlockedDetails.test.tsx`：`url` 存在时渲染链接；`key_contacts` 为字符串时按文本渲染、为数组时按联系人渲染。

---

## P2-A —— 记录面板概览数量徽标 / 首条预览 / 常驻刷新（§3.1）

**目标**：`MyRecordsPanel` 概览两张卡显示**数量徽标**（订单/解锁总数）与**首条记录预览**；下钻视图头部增加**常驻刷新按钮**（对齐远端账户弹窗内记录管理）。

**远端依据**：报告 §3.1 + `_origin_snapshot/ProcurementNoticesPool.tsx` L381-400（`refreshPaymentHistory` 用 `limit=3` 同时拉 orders + unlocks 概览）。

**改动文件与实现**：
1. `src/features/payment/hooks/useOrderHistory.ts`：当前仅加载激活 tab。新增概览摘要能力——增加一个轻量并行拉取（`fetchOrders`/`fetchUnlocks` `limit=1` 或复用 `total`）暴露 `ordersTotal / unlocksTotal / ordersFirst / unlocksFirst`；或新增独立 hook `useRecordsSummary(userKey)`（推荐，避免污染分页 hook）。
2. `src/features/payment/components/MyRecordsPanel.tsx`
   - overview 两张卡右上角渲染数量徽标 + 首条记录标题/编号预览（无数据则隐藏徽标）。
   - 下钻视图头部返回按钮旁增加常驻"刷新"按钮 → `history.refresh()`。

**i18n**：复用现有记录相关键；若新增"刷新"标签且缺失则 6 语言补齐。

**测试**：扩展 `MyRecordsPanel.test.tsx`：mock 摘要返回，断言徽标数量与首条预览渲染；点击刷新触发 `refresh`。

---

## P2-B —— 全局 `NoticeItem` 类型统一（§7）

**目标**：消除 `types/procurement.ts` 全局 `NoticeItem`（缺解锁字段）与 `features/procurement/types.ts`（含解锁字段）不一致的技术债。

**改动文件与实现**：将全局 `types/procurement.ts` 的 `NoticeItem` 补齐解锁字段，或让其 `re-export` feature 内定义（择一，保证单一事实源）；修正引用。**纯类型改动，不改运行逻辑**。

**测试**：`tsc --noEmit` 通过即可（无运行行为变更）。

---

## P2-C —— `TrainingRegisterModal` 处理（§4，孤儿文件）

**结论**：远端 `App.tsx` **未引用** `TrainingRegisterModal.tsx`，属孤儿组件，运行态无入口。**默认不迁移**（对齐远端运行态 = 无此弹窗入口）。

**可选**：若后续产品需要"弹窗报名"入口，再迁移为 `features/training/components/TrainingRegisterModal.tsx` 并接入触发按钮。本计划**不执行**，仅记录结论。

---

## P2-D —— 新增 4 语言译文抽检（§5）

**目标**：6 语言键数一致（`check-i18n` 已保证无缺键），人工抽检 fr/ru/es/ar 新增键译文质量（供应商入驻、记录面板，以及本计划新增的遮罩/拆解/链接键）。

**执行**：新增键先补齐 6 语言，运行 `npx tsx scripts/check-i18n.ts`；人工核对新语言译文准确性，必要时修订。

---

## 统一门禁与验收

**每个任务完成后执行（PowerShell 用 `;` 分隔）**：
```
npx tsc --noEmit ; npx vitest run ; npx tsx scripts/check-i18n.ts
```

**验收清单**：
- [ ] P0：CRM 跟进录入真正落库并即时回显，刷新后仍在。
- [ ] P1-A：采购详情内嵌多套餐面板可拉取套餐、创建订单、mock/真实支付、轮询对账；免费用尽自动弹面板。
- [ ] P1-B：未解锁隐藏核心信息并显示遮罩框，解锁后展示真实内容。
- [ ] P1-C：解锁后显示"投标拆解建议"4 要点。
- [ ] P1-D：交付卡有"打开公告"链接；`key_contacts` 两形态均正确渲染。
- [ ] P2-A：概览卡显示数量徽标 + 首条预览；下钻有常驻刷新。
- [ ] P2-B：全局与 feature `NoticeItem` 统一，`tsc` 通过。
- [ ] P2-C：确认 `TrainingRegisterModal` 结论（默认不迁移）。
- [ ] P2-D：6 语言键一致，新增键译文抽检通过。
- [ ] 全程 `tsc / vitest / check-i18n` 全绿；改动留在 `port/notice-payment`，不提交、不推送。

---

## 附：远端参考快照

- `.qoder/_origin_snapshot/ProcurementNoticesPool.tsx`：付费面板 / 遮罩 / 拆解建议 / 交付卡原始实现。
- `.qoder/_origin_snapshot/App.tsx`：`addCrmFollowUpLog`（L644-670）、`TrainingRegisterModal` 未引用佐证。
- `.qoder/_origin_snapshot/TrainingRegisterModal.tsx`：孤儿弹窗组件原文。
